using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Truque : Carta
{
    public Truque (string texto, string codigoID, string nomeImagem, AtributosSecundarios atributos) : base(TipoCarta.Truque, texto, codigoID, nomeImagem, atributos)
    {

    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
